[distutils]
index-servers =
    pypi
    testpypi

[pypi]
repository = https://upload.pypi.org/legacy
username=${{ secrets.PYPI_NAME }}
password=${{ secrets.PYPI_API_TOKEN }}

[testpypi]
repository = https://test.pypi.org/legacy/
